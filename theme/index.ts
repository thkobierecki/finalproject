import colors from "./colors";
import radius from "./radius";
import shadows from "./shadows";
import sizes from "./sizes";
import text, { common } from "./text";

export default {
  fontFamily: common.fontFamily,
  colors,
  radius,
  shadows,
  sizes,
  text,
};
